{
	"name": "Dotfiles",
	"build" : {
		"dockerfile": "Dockerfile",
		"context": "..",
	},
	"features": {
		"ghcr.io/devcontainers/features/sshd:1": { // Support 'gh codespace ssh' in remote containers
			"version": "latest"
		},
	},
	"postCreateCommand": "bash $HOME/.mh-bootstrap/refresh-for-new-container.sh --devcontainer",
}
