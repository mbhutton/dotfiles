# Desired APT packages.
# Read by impl-apt.sh.
# Comments, leading and trailing whitespace, and empty lines are stripped.
#
# NOTE: Any packages added using this file will remain installed in the system until manually uninstalled.

# Common packages
procps              # free, kill, pgrep, ps, etc
vim                 # Simpler than Neovim. Installed here rather than Nix because commonly APT installed already
xz-utils            # Required by Nix Home Manager setup

{{ if .mh.useRuby }}
# Required for building Ruby in Mise
build-essential
libffi-dev
libssl-dev
libyaml-dev
zlib1g-dev
{{ end }}
