# Git proper
alias t=git
alias td='git diff'
alias ts='git status'

# Git related tools
alias lg=lazygit
alias tg='tig'
{{ if eq .chezmoi.os "darwin" -}}
alias tx='open . -a GitX'
{{ else -}}
alias tx='echo "GitX is only available on macOS"'
{{ end -}}
