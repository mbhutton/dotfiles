# shellcheck shell=bash

if [ -n "$INTELLIJ_ENVIRONMENT_READER" ]; then
  # Becuase the reader script needs to do a redirect overwriting an existing file
  setopt clobber
else
  command -v fzf > /dev/null && eval "$(fzf --zsh)"
fi
