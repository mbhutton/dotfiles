# Git proper
alias t=git
alias td='git diff'
alias ts='git status'

# Git related tools
alias lg=lazygit
alias tg='tig'

if [[ -d "/Applications/GitX.app" ]]; then
  alias tx='open . -a GitX'
else
  alias tx='echo "GitX.app not installed"'
fi
